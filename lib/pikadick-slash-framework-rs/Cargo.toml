[package]
name = "pikadick-slash-framework"
version = "0.0.0"
edition = "2021"

[dependencies]
serenity = { version = "0.10.10", features = [ "client", "rustls_backend", "builder", "gateway", "model", "unstable_discord_api" ], default-features = false }
