[package]
name = "async-rusqlite"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
rusqlite = "0.30.0"
thiserror = "1.0.52"
tokio = { version = "1.35.1", features = [ "rt", "sync" ] }

[features]
bundled = [ "rusqlite/bundled" ]
